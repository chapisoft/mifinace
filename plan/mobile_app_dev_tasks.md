# DANH MỤC TASK KỸ THUẬT PHÂN HỆ MOBILE BMF

---

## 1. MA TRẬN ĐẶC TẢ CHI TIẾT 62 TASK KỸ THUẬT

### 1.1. Phân hệ Mobile BFF Gateway

| Mã & Tên Task | File / Class Chính | Nội dung & Luồng Xử lý Kỹ thuật | Tiêu chí Nghiệm thu Chi tiết (DoD) |
| :--- | :--- | :--- | :--- |
| `TASK-BFF-01.1`<br/>**Khởi tạo dự án Clean Architecture** | `pom.xml`<br/>`bmf-domain`<br/>`bmf-app`<br/>`bmf-infra`<br/>`bmf-api` | • Tạo POM cha và 4 module phân tầng theo chuẩn Clean Architecture.<br/>• Khai báo dependencies: Java 21 LTS, Spring Boot 3.3.3, MapStruct 1.6, Lombok.<br/>• Cấu hình plugin Maven Compiler hỗ trợ tính năng Java 21. | • Lệnh `mvn clean compile` thành công 100% không cảnh báo.<br/>• Cấu trúc 4 module phân định rõ ràng ranh giới phụ thuộc: `api` → `app` → `domain` ← `infra`. |
| `TASK-BFF-01.2`<br/>**Cấu hình Virtual Threads, OpenAPI & Log** | `OpenApiConfig`<br/>`logback-spring`<br/>`application.yml` | • Bật Virtual Threads: `spring.threads.virtual.enabled: true`.<br/>• Cấu hình SpringDoc OpenAPI 3.0 hiển thị Swagger UI tại `/swagger-ui.html`.<br/>• Thiết lập Logback xuất log cấu trúc JSON ECS kèm TraceId/SpanId. | • Swagger UI hiển thị đầy đủ metadata, nhóm phân loại API.<br/>• Log console xuất đúng định dạng JSON ECS, tự động sinh mã TraceId cho từng request để truy vết lỗi. |
| `TASK-BFF-01.3`<br/>**GlobalExceptionHandler RFC 7807** | `GlobalHandler`<br/>`ErrorResponse`<br/>`BizException` | • Tạo `@RestControllerAdvice` bắt toàn bộ Exception hệ thống.<br/>• Bắt `MethodArgumentNotValidException`, `BusinessException`, `Exception`.<br/>• Chuẩn hóa mã lỗi trả về: `errorCode`, `message`, `traceId`, `timestamp`. | • 100% phản hồi lỗi trả về đúng chuẩn Problem Details.<br/>• Tuyệt đối không để lộ stack trace nội bộ ra client.<br/>• Bắt đúng mã lỗi HTTP: 400 (Validation), 401, 403, 404, 409, 500. |
| `TASK-BFF-02.1`<br/>**Entity, Enum & Repo thiết bị di động** | `MobileDevice`<br/>`UserType`<br/>`DeviceRepo` | • Khai báo Enum `UserType` (AGENT, CUSTOMER), `PlatformType` (IOS, ANDROID).<br/>• Ánh xạ bảng `SYS_MOBILE_DEVICE` (deviceId, userId, deviceToken, isActive).<br/>• Viết các hàm tìm kiếm thiết bị bằng `JdbcClient`. | • Unit Test CRUD thiết bị trên CSDL SQL Server pass 100%.<br/>• Khóa thiết bị `isActive = 0` hoạt động chính xác.<br/>• Tự động cập nhật `lastActiveTime` khi có tương tác mới. |
| `TASK-BFF-02.2`<br/>**JwtTokenProvider cấp phát Token JJWT** | `JwtProvider`<br/>`JwtProperties`<br/>`TokenPayload` | • Ký số Access Token (15 phút) bằng thuật toán HMAC-SHA256 (JJWT 0.12.6).<br/>• Payload chứa các claims: `sub`, `jti`, `userType`, `deviceId`.<br/>• Sinh Refresh Token ngẫu nhiên (UUIDv4) lưu vào Redis với TTL = 30 ngày. | • Ký và giải mã Token JWT chính xác, không lỗi font chữ.<br/>• Access Token tự động hết hạn sau đúng 900 giây.<br/>• Refresh Token được lưu an toàn trong Redis kèm hạn dùng. |
| `TASK-BFF-02.3`<br/>**JwtAuthFilter & Redisson Blacklist** | `JwtAuthFilter`<br/>`SecurityConfig`<br/>`BlacklistService` | • Tạo Filter kế thừa `OncePerRequestFilter` trích xuất Bearer Token.<br/>• Kiểm tra claim `jti` trong Redis bucket `BLACKLIST_TOKEN:{jti}`.<br/>• Thiết lập `SecurityContextHolder` nếu Token hợp lệ; từ chối `401` nếu bị Blacklist. | • Token trong Blacklist bị từ chối truy cập ngay lập tức.<br/>• Request không có token hoặc token sai định dạng trả về `401`.<br/>• Thời gian kiểm tra blacklist trên Redis < 2ms. |
| `TASK-BFF-02.4`<br/>**API Đăng nhập Cán bộ tín dụng** | `AuthController`<br/>`AgentLoginReq`<br/>`AuthResponse` | • Endpoint `POST /api/v1/auth/agent-login`.<br/>• So khớp mật khẩu với bảng `HT_NGUOIDUNG` bằng `BCryptPasswordEncoder`.<br/>• Kiểm tra trạng thái thiết bị trong `SYS_MOBILE_DEVICE` và cấp phát cặp Token. | • Đăng nhập thành công trả về `200 OK` kèm Access Token.<br/>• Từ chối `403` nếu tài khoản cán bộ bị khóa hoặc máy lạ.<br/>• Cập nhật đúng FCM Token của cán bộ vào CSDL. |
| `TASK-BFF-02.5`<br/>**API Đăng nhập Khách hàng qua NRC** | `AuthController`<br/>`CustLoginReq`<br/>`CustomerAuth` | • Endpoint `POST /api/v1/auth/customer-login`.<br/>• Tra cứu khách hàng theo số thẻ NRC trong bảng `KH_THANHVIEN`.<br/>• Xác thực mã PIN đã băm và cấp phát Token cho khách hàng. | • Khách hàng đăng nhập thành công bằng số NRC và mã PIN 6 số.<br/>• Khóa tạm thời 15 phút nếu nhập sai mã PIN quá 5 lần liên tiếp.<br/>• Trả về thông tin tóm tắt của thành viên và danh sách khoản vay. |
| `TASK-BFF-02.6`<br/>**API Refresh Token & Đăng xuất Logout** | `AuthController`<br/>`RefreshReq`<br/>`LogoutReq` | • Endpoint `POST /api/v1/auth/refresh-token`: Làm mới Access Token.<br/>• Endpoint `POST /api/v1/auth/logout`: Đưa `jti` của Access Token vào Redis Blacklist và xóa Refresh Token khỏi Redis. | • Cấp mới Access Token thành công khi Refresh Token hợp lệ.<br/>• Token cũ sau khi Logout bị vô hiệu hóa hoàn toàn trên toàn hệ thống.<br/>• Xóa sạch phiên đăng nhập trên Redis. |
| `TASK-BFF-03.1`<br/>**HikariCP kết nối Cục bộ SQL Server NG-mFINA** | `DataSourceConfig`<br/>`application.yml` | • Cấu hình `HikariDataSource` kết nối CSDL `NG-mFINA-BMF_20180402` qua **mạng nội bộ cục bộ (Localhost / Private LAN Port 1433)**.<br/>• Thiết lập `maximumPoolSize = 30`, `minimumIdle = 10`, `connectionTimeout = 30s`.<br/>• Tắt `autoCommit` cho các luồng nghiệp vụ tài chính. | • Kết nối CSDL SQL Server 2017 nội bộ ổn định, không rò rỉ connection.<br/>• Tốc độ mượn kết nối từ HikariCP pool qua Local LAN < 1ms.<br/>• Tuyệt đối không mở port CSDL 1433 ra Public Internet. |
| `TASK-BFF-03.2`<br/>**LoanJdbcRepository truy vấn nợ & Cụm/Tổ** | `LoanJdbcRepo`<br/>`CenterDto`<br/>`ScheduleDto` | • Sử dụng Spring 6 `JdbcClient` viết câu lệnh SQL truy vấn trực tiếp.<br/>• Truy vấn danh mục Cụm/Tổ và bảng kê nợ đến hạn theo `Ma_Cum_To`.<br/>• Ánh xạ dữ liệu sang Java 21 Records với độ trễ < 50ms. | • Lấy danh mục 100 thành viên trong Cụm/Tổ < 50ms.<br/>• Số liệu gốc, lãi, phí bảo hiểm hiển thị khớp 100% với Core.<br/>• Câu lệnh SQL được tối ưu hóa qua chỉ mục Index. |
| `TASK-BFF-03.3`<br/>**Apache CXF Client gọi Cục bộ WCF TinDungService** | `TinDungClient`<br/>`WcfConfig`<br/>`ResilienceConfig` | • Sinh SOAP Client từ WSDL của WCF `TinDungService` hiện hữu qua **mạng cục bộ (Localhost / Private LAN Port 8000/8001)**.<br/>• Gọi các hàm lõi: `GiaiNgan`, `ThuNo`, `PhanLoaiNo`.<br/>• Bọc bằng Resilience4j Circuit Breaker (tự động thử lại 3 lần và ngắt mạch khi lỗi). | • Gọi WCF gạch nợ cục bộ thành công với độ trễ thấp < 30ms, hạch toán đúng số tiền MMK.<br/>• Tự động ngắt mạch (Open State) sau 5 lần lỗi liên tiếp để bảo vệ Core.<br/>• WCF Service được bảo vệ trong Private Zone, không tiếp xúc Public Internet. |
| `TASK-BFF-03.4`<br/>**CXF Client gọi Cục bộ WCF Kế toán & Tiết kiệm** | `KeToanClient`<br/>`TietKiemClient` | • Kết nối WCF SOAP `KeToanService` (hạch toán kép) và `TietKiemService` qua **mạng nội bộ cục bộ**.<br/>• Thực hiện ghi nhận bút toán Nợ/Có trên sổ cái kế toán Core. | • Bút toán hạch toán kế toán được sinh tự động sau khi thu tiền.<br/>• Cân đối tuyệt đối giữa tổng Nợ và tổng Có (sai số = 0 MMK).<br/>• Giao tiếp nội bộ an toàn, ghi nhận chính xác mã giao dịch vào sổ phụ kế toán. |
| `TASK-BFF-04.1`<br/>**IdempotencyFilter bẫy trùng lặp giao dịch** | `IdempotencyFilter`<br/>`IdempotencyConfig` | • Bắt buộc Header `X-Idempotency-Key` (UUIDv4) trên `POST`, `PUT`, `DELETE`.<br/>• Dùng `RBucket` Redisson: `setIfAbsent("PROCESSING", 120s)`.<br/>• Nếu trùng → Trả về `409 Conflict`; nếu đã hoàn tất → Trả về cached response. | • Chặn đứng 100% request trùng lặp gửi dồn dập từ Client.<br/>• Request gửi lại nhận đúng kết quả của giao dịch trước mà không trừ tiền 2 lần.<br/>• Tự động giải phóng khóa sau 120 giây nếu xảy ra sự cố mạng. |
| `TASK-BFF-04.2`<br/>**DistributedLockHelper bọc Redisson RLock** | `LockHelper`<br/>`RedissonConfig` | • Xây dựng helper thực thi khối lệnh an toàn với `RLock`.<br/>• Khóa theo mã khế ước (`"LOAN:" + loanCode`) hoặc sổ tiết kiệm.<br/>• Tự động giải phóng khóa trong khối `finally` sau khi hoàn tất giao dịch. | • 100 luồng đồng thời vào 1 món nợ chỉ có duy nhất 1 luồng thành công.<br/>• Không xảy ra hiện tượng Deadlock trên CSDL.<br/>• Báo lỗi rõ ràng `ERR_RESOURCE_LOCKED` khi hết thời gian chờ khóa. |
| `TASK-BFF-04.3`<br/>**RateLimitingFilter Token Bucket trên Redis** | `RateLimitFilter`<br/>`RateLimitRule` | • Sử dụng `RRateLimiter` của Redisson quản lý hạn mức gọi API theo IP / UserId.<br/>• Giới hạn 20 requests/giây cho API nghiệp vụ, 5 requests/phút cho API đăng nhập.<br/>• Vượt ngưỡng trả về `429 Too Many Requests` kèm Header `Retry-After`. | • Chặn đứng tấn công brute-force mật khẩu và DDoS tầng ứng dụng.<br/>• Trả về mã lỗi HTTP 429 kèm thời gian chờ thử lại chính xác.<br/>• Không ảnh hưởng đến các người dùng hợp lệ khác trong hệ thống. |
| `TASK-BFF-05.1`<br/>**API Cán bộ: Danh sách Center/Group** | `AgentController`<br/>`AgentService`<br/>`CenterResponse` | • Endpoint `GET /api/v1/agent/centers` (Lấy UserId từ SecurityContext).<br/>• Tra cứu danh sách các Center/Group do cán bộ phụ trách.<br/>• Trả về thông tin: Tên Cụm, tên Tổ trưởng, số thành viên, số tiền cần thu hôm nay. | • Trả về danh sách Cụm/Tổ đầy đủ theo đúng phân cấp địa bàn.<br/>• Tổng hợp chính xác số tiền cần thu trong ngày theo đơn vị MMK.<br/>• Thời gian phản hồi API < 100ms. |
| `TASK-BFF-05.2`<br/>**API Cán bộ: Bảng kê lịch nợ Cụm/Tổ** | `AgentController`<br/>`GroupSchedule` | • Endpoint `GET /api/v1/agent/groups/{groupId}/schedule`.<br/>• Truy vấn lịch nợ đến hạn của tất cả thành viên trong Cụm/Tổ.<br/>• Chi tiết từng người: Nợ gốc, nợ lãi, phí bảo hiểm, tiết kiệm bắt buộc (MMK). | • Trả về bảng kê thu nợ chính xác từng đồng MMK.<br/>• Phân tách rõ ràng nợ gốc, lãi, bảo hiểm của từng thành viên.<br/>• Đánh dấu trạng thái các khoản nợ đã được gạch nợ trước đó. |
| `TASK-BFF-05.3`<br/>**API Cán bộ: Lập phiếu thu tiền nợ MMK** | `AgentController`<br/>`CollectReq`<br/>`ReceiptResp` | • Endpoint `POST /api/v1/agent/repayment/collect`.<br/>• Kiểm tra Idempotency Key, chiếm Redisson Lock theo `loanCode`.<br/>• Gọi WCF gạch nợ, hạch toán kế toán và trả về mã biên lai điện tử. | • Gạch nợ thành công, sinh biên nhận và trừ nợ tức thời trên Core.<br/>• Trả về mã giao dịch duy nhất để in hóa đơn nhiệt Bluetooth.<br/>• Tự động rollback giao dịch nếu bước hạch toán kế toán bị lỗi. |
| `TASK-BFF-05.4`<br/>**API Cán bộ: Nộp hồ sơ vay & Thẩm định** | `AgentController`<br/>`LoanApplyReq`<br/>`LoanApplyResp` | • Endpoint `POST /api/v1/agent/loans/apply`.<br/>• Nhận dữ liệu: Số NRC, số tiền vay, kỳ hạn, ảnh NRC, tọa độ GPS, chữ ký số.<br/>• Lưu chứng từ thực địa và tạo bản ghi hồ sơ vay chờ phê duyệt. | • Hồ sơ vay vốn được tạo thành công kèm đầy đủ chứng từ số hóa.<br/>• Lưu trữ an toàn ảnh chụp NRC, ảnh khảo sát nhà và chữ ký điện tử.<br/>• Tự động tính điểm chấm điểm hộ nghèo đa chiều theo thuật toán Core. |
| `TASK-BFF-05.5`<br/>**API Cán bộ: Thu tiền tiết kiệm & Mở sổ** | `AgentController`<br/>`SavingReq`<br/>`OpenSavingReq` | • Endpoint `POST /api/v1/agent/savings/collect`, `POST /api/v1/agent/savings/open`.<br/>• Chiếm Redisson Lock theo mã sổ tiết kiệm.<br/>• Gọi WCF Tiết kiệm để nạp tiền hoặc mở sổ mới tại buôn làng. | • Tiền gửi được hạch toán ngay vào sổ tiết kiệm của thành viên.<br/>• Cập nhật số dư mới tức thời trên hệ thống.<br/>• Sinh mã phiếu thu tiền gửi để in biên nhận tại chỗ. |
| `TASK-BFF-05.6`<br/>**API Cán bộ: Tiếp nhận hồ sơ bảo hiểm** | `AgentController`<br/>`InsuranceClaimReq` | • Endpoint `POST /api/v1/agent/insurance/claim`.<br/>• Tiếp nhận thông tin sự cố rủi ro và ảnh chứng từ y tế / xác nhận trưởng làng.<br/>• Lưu hồ sơ vào bảng `BH_HOSOBHTH` để Township thẩm định chi trả. | • Hồ sơ trợ cấp được gửi về văn phòng chi nhánh thành công.<br/>• Đính kèm đầy đủ ảnh chứng từ y tế rõ nét.<br/>• Sinh mã tiếp nhận hồ sơ để cán bộ và khách hàng cùng theo dõi. |
| `TASK-BFF-05.7`<br/>**API Cán bộ: Sinh mã QR bàn giao quỹ** | `AgentController`<br/>`CashHandoverReq`<br/>`CashHandoverResp` | • Endpoint `POST /api/v1/agent/cash/handover-qr`.<br/>• Tổng hợp toàn bộ số tiền mặt cán bộ đã thu trong ngày.<br/>• Ký số payload và sinh mã QR bảo mật để thủ quỹ tại quầy quét đối soát nhập két. | • Sinh mã QR chứa đúng tổng tiền thu và danh sách phiếu thu trong ngày.<br/>• Mã QR có chữ ký số chống giả mạo số liệu.<br/>• Thủ quỹ tại Township quét mã khớp 100% số tiền kiểm đếm. |
| `TASK-BFF-05.8`<br/>**API Khách hàng: Tra cứu hợp đồng vay** | `CustController`<br/>`CustLoanResp` | • Endpoint `GET /api/v1/customer/loans` (Lấy UserId từ Token).<br/>• Tra cứu danh sách các khoản vay đang hoạt động của khách hàng.<br/>• Tính tổng dư nợ gốc còn lại, số tiền phải trả kỳ tới và ngày đến hạn. | • Trả về danh sách khoản vay trực quan và chính xác.<br/>• Hiển thị đúng số tiền phải trả kỳ tới và ngày đến hạn chính xác.<br/>• Bảo đảm tính riêng tư: Khách hàng chỉ xem được dữ liệu của chính mình. |
| `TASK-BFF-05.9`<br/>**API Khách hàng: Lịch trả nợ 5 nhóm nợ** | `CustController`<br/>`ScheduleDetailResp` | • Endpoint `GET /api/v1/customer/loans/{loanId}/schedule`.<br/>• Truy vấn lịch trả nợ toàn khóa của hợp đồng.<br/>• Phân loại nhóm nợ (Nhóm 1 đến Nhóm 5) theo quy định FRD Myanmar. | • Hiển thị đầy đủ bảng lịch trả nợ theo từng kỳ từ kỳ đầu đến kỳ cuối.<br/>• Phân loại chính xác 5 nhóm nợ theo số ngày quá hạn thực tế chuẩn FRD.<br/>• Hiển thị chi tiết số tiền đã trả và số tiền còn nợ từng kỳ. |
| `TASK-BFF-05.10`<br/>**API Khách hàng: Sổ tiết kiệm & Lãi tích lũy** | `CustController`<br/>`CustSavingResp` | • Endpoint `GET /api/v1/customer/savings`.<br/>• Lấy danh sách sổ tiết kiệm (Bắt buộc, Tự nguyện, Tích lũy).<br/>• Tính số tiền lãi dồn tích thực tế hàng ngày bằng MMK. | • Khách hàng theo dõi được chính xác số dư gốc và tiền lãi dồn tích.<br/>• Hiển thị lịch sử các lần nộp tiền gửi rõ ràng, minh bạch.<br/>• Số tiền lãi tính toán khớp từng đồng MMK với bảng tính Core. |
| `TASK-BFF-05.11`<br/>**API Khách hàng: Mở sổ tiết kiệm tích lũy** | `CustController`<br/>`CustOpenSavingReq` | • Endpoint `POST /api/v1/customer/savings/open`.<br/>• Nhận thông tin gói kỳ hạn (3, 6, 12 tháng) và số tiền gửi ban đầu.<br/>• Tạo sổ tiết kiệm mới ở trạng thái chờ nạp tiền qua ví điện tử. | • Sinh mã sổ tiết kiệm mới sẵn sàng nhận tiền nạp từ ví điện tử.<br/>• Áp dụng đúng biểu lãi suất tiết kiệm hiện hành của BMF.<br/>• Trả về thông tin gói tiết kiệm và mã QR để nộp tiền. |
| `TASK-BFF-05.12`<br/>**API Khách hàng: Quyền lợi & Nộp bảo hiểm** | `CustController`<br/>`CustClaimReq` | • Endpoint `POST /api/v1/customer/insurance/claim`.<br/>• Khách hàng tự điền thông tin rủi ro và tải ảnh hóa đơn viện phí từ điện thoại.<br/>• Ghi nhận hồ sơ yêu cầu trợ cấp vào hệ thống. | • Khách hàng nộp hồ sơ thành công và nhận mã tra cứu tiến độ.<br/>• Nhận thông báo xác nhận đã tiếp nhận hồ sơ qua Notification.<br/>• Theo dõi được trạng thái hồ sơ (Chờ duyệt / Đã duyệt / Đã chi trả). |
| `TASK-BFF-06.1`<br/>**Module sinh mã MMQR động chuẩn CBM** | `MmqrGenerator`<br/>`MmqrPayload`<br/>`PaymentController` | • Endpoint `GET /api/v1/payments/mmqr`.<br/>• Đóng gói chuỗi TLV chuẩn EMVCo chứa tài khoản BMF và số tiền nợ chính xác.<br/>• Sử dụng thư viện ZXing xuất chuỗi và ảnh PNG QR Code động. | • Quét mã MMQR trên app ngân hàng hiển thị đúng số tiền và tài khoản BMF.<br/>• Chuỗi mã QR tuân thủ 100% chuẩn EMVCo của Ngân hàng Trung ương CBM.<br/>• Nhúng đúng mã tham chiếu hợp đồng để đối soát tự động. |
| `TASK-BFF-06.2`<br/>**Webhook tiếp nhận thanh toán KBZPay** | `WebhookController`<br/>`KbzPayDto`<br/>`PaymentService` | • Endpoint `POST /api/v1/payments/webhook/kbzpay`.<br/>• Xác thực chữ ký HMAC-SHA256 trong Header với Secret Key của KBZPay.<br/>• Bẫy trùng lặp, chiếm Redisson Lock và gọi gạch nợ tự động vào Core. | • Gạch nợ tự động trong vòng 500ms khi nhận webhook từ KBZPay.<br/>• Từ chối ngay lập tức các request có chữ ký số không hợp lệ.<br/>• Trả về đúng định dạng response `{"code": "0", "msg": "SUCCESS"}` cho đối tác. |
| `TASK-BFF-06.3`<br/>**Webhook tiếp nhận thanh toán WavePay** | `WebhookController`<br/>`WavePayDto` | • Endpoint `POST /api/v1/payments/webhook/wavepay`.<br/>• Xác thực chữ ký số WavePay.<br/>• Bẫy giao dịch trùng lặp, gạch nợ vào Core và sinh thông báo đẩy cho khách hàng. | • Gạch nợ thành công và bắn thông báo xác nhận thanh toán qua WavePay.<br/>• Khách hàng nhận được thông báo rung điện thoại ngay sau khi chuyển tiền.<br/>• Cập nhật trạng thái lịch nợ thành `PAID` tức thời. |
| `TASK-BFF-06.4`<br/>**Webhook tiếp nhận AYA Pay & MytelPay** | `WebhookController`<br/>`AyaPayDto`<br/>`MytelPayDto` | • Endpoint `POST /api/v1/payments/webhook/ayapay`, `/mytelpay`.<br/>• Chuẩn hóa quy trình gạch nợ tự động theo mẫu Adapter chung. | • Xử lý gạch nợ thông suốt từ 4 ví điện tử phổ biến tại Myanmar.<br/>• Xử lý đồng bộ, ghi nhận rõ nguồn thanh toán trong sổ cái kế toán.<br/>• Đối soát tự động cuối ngày không phát sinh sai lệch. |

---

### 1.2. Phân hệ BMF Agent App

| Mã & Tên Task | File / Class Chính | Nội dung & Luồng Xử lý Kỹ thuật | Tiêu chí Nghiệm thu Chi tiết (DoD) |
| :--- | :--- | :--- | :--- |
| `TASK-AGENT-01.1`<br/>**Khởi tạo Flutter FSD Clean Architecture** | `pubspec.yaml`<br/>`app_locator`<br/>`app_observer` | • Khởi tạo dự án Flutter 3.24+ theo cấu trúc FSD (Feature-Sliced Design).<br/>• Khai báo dependencies: `flutter_bloc`, `get_it`, `dio`, `freezed`.<br/>• Cấu hình Service Locator Dependency Injection và Bloc Observer. | • Ứng dụng khởi chạy thành công trên Android Emulator và iOS Simulator.<br/>• Quản lý trạng thái bằng Bloc hoạt động mượt mà, không giật lag.<br/>• Dependency Injection đăng ký đầy đủ các Service và Repository. |
| `TASK-AGENT-01.2`<br/>**Font Pyidaungsu & Đa ngôn ngữ i18n** | `pubspec.yaml`<br/>`AppTypography`<br/>`app_my.arb` | • Nhúng font chữ `Pyidaungsu-Regular.ttf` và `Pyidaungsu-Bold.ttf`.<br/>• Thiết lập `AppTypography` với font chữ tiếng Myanmar Unicode mặc định.<br/>• Viết từ điển song ngữ tiếng Myanmar và tiếng Anh. | • Hiển thị tiếng Myanmar chuẩn xác 100%, không bị vỡ dấu hay lỗi ký tự.<br/>• Chuyển đổi qua lại giữa tiếng Myanmar và tiếng Anh không bị tràn viền UI.<br/>• Tự động điều chỉnh kích thước chữ theo cài đặt hệ thống máy. |
| `TASK-AGENT-01.3`<br/>**DioClient, Auth & Retry Interceptor** | `DioClient`<br/>`AuthInterceptor`<br/>`RetryInterceptor` | • Tự động gắn Bearer Token vào Header `Authorization`.<br/>• Khi nhận mã `401`: Khóa hàng đợi request, tự động gọi API Refresh Token ngầm.<br/>• Tự động thử lại 3 lần với Exponential Backoff khi chập chờn mạng. | • Tự động làm mới Token trong suốt mà không đẩy người dùng ra màn hình đăng nhập.<br/>• Hàng đợi các request đồng thời được giữ lại và gửi tiếp sau khi có token mới.<br/>• Không bị treo ứng dụng khi mạng bị ngắt đột ngột. |
| `TASK-AGENT-02.1`<br/>**Drift ORM & SQLite SQLCipher AES-256** | `AppDatabase`<br/>`KeyStorage`<br/>`DatabaseModule` | • Sinh khóa ngẫu nhiên AES-256 32 bytes lưu trong Keystore/Keychain.<br/>• Mở kết nối Drift Database với chuỗi khóa mã hóa `password`.<br/>• Thiết lập migration schema version 1. | • CSDL cục bộ SQLite được mã hóa AES-256 hoàn toàn.<br/>• Trích xuất tệp `.db` ra máy tính không đọc được dữ liệu nếu không có khóa.<br/>• Tốc độ mở kết nối và giải mã CSDL < 200ms lúc khởi động app. |
| `TASK-AGENT-02.2`<br/>**Bảng Drift danh mục Cụm/Tổ & Lịch nợ** | `CustomersTable`<br/>`GroupsTable`<br/>`SchedulesTable` | • Định nghĩa các bảng danh mục: `LocalCustomers`, `LocalGroups`, `LocalLoanSchedules`.<br/>• Tạo DAO truy vấn danh sách thành viên theo `groupId`.<br/>• Tạo DAO truy vấn lịch nợ đến hạn theo ngày. | • Tạo thành công cấu trúc bảng và đánh Index cục bộ.<br/>• Truy vấn danh sách 100 thành viên trong CSDL máy cán bộ < 10ms.<br/>• Hỗ trợ tìm kiếm theo tên và số NRC không dấu/có dấu mượt mà. |
| `TASK-AGENT-02.3`<br/>**Bảng Drift giao dịch & Hàng đợi Sync** | `TransactionsTable`<br/>`SyncQueueTable`<br/>`SyncDao` | • Định nghĩa bảng `LocalTransactions` lưu phiếu thu tiền mặt tại chỗ.<br/>• Định nghĩa bảng `LocalSyncQueue` lưu JSON payload chờ đẩy lên máy chủ.<br/>• Viết DAO thêm, sửa, cập nhật trạng thái `PENDING`, `SYNCED`, `FAILED`. | • Ghi nhận giao dịch nguyên tử (Atomic Transaction): Không bị mất dữ liệu khi sập nguồn.<br/>• Cập nhật trạng thái hàng đợi đồng bộ cục bộ chính xác.<br/>• Hỗ trợ xóa dọn dẹp các bản ghi đã đồng bộ quá 30 ngày. |
| `TASK-AGENT-03.1`<br/>**Màn hình Đăng nhập Cán bộ tín dụng** | `LoginScreen`<br/>`LoginBloc`<br/>`LoginState` | • Form đăng nhập: Tên đăng nhập, Mật khẩu.<br/>• Validate form bằng Bloc, gọi API đăng nhập.<br/>• Lưu Access/Refresh Token vào SecureStorage và chuyển sang Dashboard. | • Đăng nhập mượt mà, hiển thị thông báo lỗi tiếng Myanmar khi sai mật khẩu.<br/>• Vô hiệu hóa nút đăng nhập khi đang gửi request tránh bấm nhiều lần.<br/>• Lưu thông tin phiên làm việc an toàn vào bộ nhớ mã hóa. |
| `TASK-AGENT-03.2`<br/>**Ràng buộc Thiết bị & Sinh trắc học** | `BiometricService`<br/>`DeviceDialog` | • Lấy thông tin định danh phần cứng (`device_info_plus`).<br/>• Đăng nhập nhanh bằng vân tay / FaceID nếu phiên còn hiệu lực (`local_auth`).<br/>• Hiển thị cảnh báo nếu thiết bị chưa được kích hoạt bởi IT. | • Đăng nhập bằng sinh trắc học < 1 giây, tiện lợi ngoài thực địa.<br/>• Chặn đăng nhập nếu chạy trên thiết bị lạ chưa được phê duyệt.<br/>• Tự động khóa màn hình sau 5 phút không tương tác. |
| `TASK-AGENT-04.1`<br/>**Màn hình Dashboard Tổng quan Cán bộ** | `DashboardScreen`<br/>`DashboardBloc` | • Hiển thị các thẻ thống kê tổng quan: Số Cụm/Tổ, số thành viên cần thu hôm nay.<br/>• Thanh tiến độ thu nợ trong ngày.<br/>• Nút lối tắt: Tải dữ liệu ngoại tuyến, Bắt đầu buổi sinh hoạt Cụm/Tổ. | • Giao diện trực quan, tải dữ liệu từ CSDL cục bộ tức thời < 100ms.<br/>• Thống kê chính xác số tiền đã thu và số tiền còn lại trong ngày.<br/>• Hiển thị cảnh báo màu vàng nếu chưa đồng bộ dữ liệu đầu ngày. |
| `TASK-AGENT-04.2`<br/>**Màn hình Danh sách Cụm/Tổ (Center/Group)** | `CenterListScreen`<br/>`GroupWidget` | • Hiển thị danh sách Cụm/Tổ dạng cây phân cấp hành chính.<br/>• Tìm kiếm nhanh theo tên Cụm, tên Tổ trưởng, buôn làng.<br/>• Bấm vào Tổ để mở bảng kê thu nợ chi tiết. | • Tìm kiếm và lọc Cụm/Tổ mượt mà 60 FPS.<br/>• Hiển thị số lượng thành viên và số tiền nợ của từng tổ rõ ràng.<br/>• Đánh dấu biểu tượng hoàn thành khi tổ đã thu đủ 100%. |
| `TASK-AGENT-04.3`<br/>**Bảng kê Thu nợ Cụm/Tổ** | `CollectionScreen`<br/>`CollectionBloc` | • Hiển thị danh sách thành viên với badge trạng thái: Chưa thu, Đã thu, Vắng mặt.<br/>• Hiển thị tổng tiền đã thu / Tổng tiền cần thu của cả tổ.<br/>• Nút "Thu nhanh tất cả" khi cả tổ nộp đủ tiền mặt. | • Cán bộ bao quát toàn bộ tiến độ thu nợ của cả tổ trong 1 màn hình.<br/>• Tính năng "Thu nhanh tất cả" xử lý xong cả tổ trong 2 giây.<br/>• Màu sắc phân định rõ ràng giúp cán bộ không bị nhầm lẫn người chưa nộp. |
| `TASK-AGENT-04.4`<br/>**Dialog / Form Lập phiếu thu nợ MMK** | `CollectDialog`<br/>`AmountInput` | • Chọn hình thức: Thu đủ, Thu một phần (nhập số tiền), Gia hạn nợ/Vắng mặt.<br/>• Tính tức thời số tiền phân bổ: Gốc, lãi, tiết kiệm bắt buộc, phí bảo hiểm.<br/>• Tự động tính số dư nợ còn lại sau thu và chuyển sang luồng in biên lai. | • Thao tác lập phiếu cho 1 thành viên hoàn tất trong dưới 5 giây.<br/>• Tự động tính toán phân bổ số tiền chuẩn xác từng đồng MMK.<br/>• Bắt buộc chọn lý do khi báo vắng mặt hoặc xin gia hạn nợ. |
| `TASK-AGENT-04.5`<br/>**Ghi nhận giao dịch & Đẩy vào Sync Queue** | `RecordUseCase`<br/>`TransactionDao` | • Mở giao dịch CSDL cục bộ SQLite nguyên tử.<br/>• Cập nhật trạng thái lịch nợ thành `PAID` trên CSDL máy cán bộ.<br/>• Thêm 1 bản ghi `PENDING` vào `LocalSyncQueue` kèm UUID duy nhất. | • Giao dịch được lưu an toàn trên máy cán bộ ngay cả khi mất sóng hoàn toàn.<br/>• Trạng thái trên giao diện đổi sang màu xanh "Đã thu" tức thì.<br/>• Không bao giờ bị mất dữ liệu phiếu thu khi app bị tắt đột ngột. |
| `TASK-AGENT-05.1`<br/>**Quét & Kết nối máy in Bluetooth ESC/POS** | `PrinterService`<br/>`PrinterDialog` | • Quét tìm các thiết bị Bluetooth xung quanh (`flutter_blue_plus`).<br/>• Lọc danh sách máy in nhiệt mini (khổ giấy 58mm và 80mm).<br/>• Kết nối và lưu địa chỉ MAC máy in đã ghép đôi. | • Tự động kết nối lại máy in nhiệt trong vòng 2 giây khi bật Bluetooth.<br/>• Hiển thị trạng thái kết nối máy in (Đã kết nối / Chưa kết nối) trên thanh tiêu đề.<br/>• Báo lỗi rõ ràng nếu máy in hết giấy hoặc mở nắp. |
| `TASK-AGENT-05.2`<br/>**Canvas hóa đơn tiếng Myanmar & Render Bitmap** | `ReceiptCanvas`<br/>`BitmapRenderer` | • Thiết kế widget hóa đơn khổ chuẩn (384px cho khổ 58mm, 576px cho khổ 80mm).<br/>• Vẽ đầy đủ ký tự tiếng Myanmar Unicode.<br/>• Dùng `RenderRepaintBoundary` trích xuất ảnh → Chuyển thành Bitmap 1-bit. | • Render hóa đơn tiếng Myanmar sắc nét, không bị vỡ nét hay nhòe chữ.<br/>• Bố cục hóa đơn cân đối: Logo BMF, Mã giao dịch, Chi tiết tiền, Tên cán bộ.<br/>• Tốc độ chuyển đổi từ Widget sang Bitmap < 300ms. |
| `TASK-AGENT-05.3`<br/>**Đóng gói lệnh in ESC/POS & Truyền Bluetooth** | `EscPosBuilder`<br/>`ByteSender` | • Khởi tạo lệnh máy in `ESC @`.<br/>• Đóng gói lệnh in đồ họa raster `GS v 0 m xL xH yL yH data...`.<br/>• Thêm lệnh đẩy giấy `ESC d 3` và gửi theo từng chunk 512 bytes qua Bluetooth. | • Máy in mini Bluetooth in ra biên lai tiếng Myanmar hoàn chỉnh trong 3 giây.<br/>• Không bị nghẽn buffer hoặc mất dữ liệu giữa chừng khi in.<br/>• Cho phép in lại biên lai bất kỳ lúc nào từ lịch sử giao dịch. |
| `TASK-AGENT-06.1`<br/>**SyncBloc & Lắng nghe kết nối Connectivity** | `SyncBloc`<br/>`NetworkMonitor` | • Lắng nghe sự kiện thay đổi trạng thái mạng (`connectivity_plus`).<br/>• Khi có mạng trở lại → Tự động kích hoạt luồng đồng bộ Outbox.<br/>• Cập nhật biểu tượng trạng thái mạng Online/Offline trên thanh tiêu đề. | • Tự động phát hiện có mạng trở lại và kích hoạt tiến trình đồng bộ tức thời.<br/>• Chuyển đổi trạng thái Online/Offline mượt mà không gây gián đoạn thao tác.<br/>• Hiển thị thanh thông báo màu cam khi đang ở chế độ Ngoại tuyến. |
| `TASK-AGENT-06.2`<br/>**Tiến trình Pull Sync: Tải danh mục & Lịch nợ** | `PullSyncUseCase`<br/>`SyncService` | • Gọi API `GET /api/v1/agent/centers` & `GET /api/v1/agent/groups/{id}/schedule`.<br/>• Tải toàn bộ dữ liệu mới nhất từ máy chủ về máy cán bộ.<br/>• Hợp nhất vào CSDL SQLite cục bộ và cập nhật `lastSyncTime`. | • Dữ liệu lịch nợ mới nhất được tải về đầy đủ trên máy cán bộ.<br/>• Xử lý hợp nhất dữ liệu thông minh không ghi đè các giao dịch đang chờ gửi.<br/>• Hoàn tất tải dữ liệu đầu ngày trong vòng dưới 10 giây. |
| `TASK-AGENT-06.3`<br/>**Tiến trình Push Sync: Đẩy lô giao dịch cục bộ** | `PushSyncUseCase`<br/>`BatchUploader` | • Đọc toàn bộ bản ghi `PENDING` trong `LocalSyncQueue`.<br/>• Đóng gói danh sách giao dịch kèm `Idempotency-Key` gửi API Batch Upload.<br/>• Khi máy chủ trả về `200 OK`: Đánh dấu `SYNCED` và xóa khỏi hàng đợi. | • Đẩy 50 giao dịch ngoại tuyến lên máy chủ an toàn trong vòng 5 giây.<br/>• Không bao giờ bị trùng lặp giao dịch nhờ khóa Idempotency duy nhất.<br/>• Tự động thử lại theo cơ chế Exponential Backoff nếu gặp lỗi kết nối. |
| `TASK-AGENT-06.4`<br/>**Giao diện trạng thái đồng bộ (Dialog, Badge)** | `SyncWidget`<br/>`UnsyncedBadge` | • Hiển thị badge số lượng giao dịch đang chờ đồng bộ (`[12 chờ đồng bộ]`).<br/>• Dialog hiển thị tiến trình đồng bộ chi tiết dạng thanh phần trăm.<br/>• Cung cấp nút "Đồng bộ ngay". | • Cán bộ nắm rõ 100% tình trạng dữ liệu đã lên Core hay đang lưu cục bộ.<br/>• Dialog hiển thị trực quan số lượng bản ghi đã gửi / tổng số bản ghi.<br/>• Báo cáo đối soát nhanh sau khi đồng bộ thành công. |
| `TASK-AGENT-07.1`<br/>**Camera OCR Thẻ căn cước Myanmar (NRC)** | `NrcCameraScreen`<br/>`NrcOcrService` | • Bật camera với khung chữ nhật hướng dẫn đặt thẻ NRC.<br/>• Chụp ảnh và chạy bộ nhận dạng ký tự On-Device ML Kit.<br/>• Trích xuất toàn bộ chuỗi ký tự nhận diện được. | • Quét và nhận dạng văn bản trên thẻ NRC trong vòng dưới 2 giây.<br/>• Chế độ tự động chụp khi căn chỉnh thẻ khớp vào khung hình.<br/>• Hoạt động hoàn toàn ngoại tuyến không cần kết nối Internet. |
| `TASK-AGENT-07.2`<br/>**Regex bóc tách chuẩn định dạng NRC Myanmar** | `NrcParser`<br/>`NrcDto` | • Áp dụng Regex: `^([0-9]{1,2})\/([A-Za-z]+)\((N|P|E)\)([0-9]{6})$`.<br/>• Bóc tách tự động: Mã vùng, Tên Township, Loại công dân `(N)`, Số căn cước.<br/>• Tự động điền thông tin vào form hồ sơ vay vốn. | • Độ chính xác bóc tách số NRC rõ nét đạt > 95%.<br/>• Tự động chuẩn hóa chữ hoa/chữ thường và loại bỏ khoảng trắng thừa.<br/>• Báo lỗi đỏ nếu số thẻ NRC không đúng cấu trúc quốc gia Myanmar. |
| `TASK-AGENT-07.3`<br/>**Màn hình Khảo sát Hiện trạng Nhà & GPS** | `SurveyScreen`<br/>`GpsService` | • Chụp ảnh hiện trạng nhà ở, chuồng trại gia súc, đất nông nghiệp.<br/>• Lấy tọa độ GPS chính xác (Kinh độ, Vĩ độ, Bán kính sai số mét).<br/>• Nén ảnh chất lượng cao (< 300 KB/ảnh) trước khi lưu trữ. | • Lưu trữ ảnh khảo sát thực địa kèm tọa độ GPS vị trí buôn làng.<br/>• Đóng dấu thời gian (Timestamp) và tọa độ trực tiếp lên góc ảnh.<br/>• Dung lượng ảnh tối ưu giúp đồng bộ nhanh qua mạng 3G/4G yếu. |
| `TASK-AGENT-07.4`<br/>**Widget Ký Hợp đồng Tín dụng (e-Signature)** | `SignaturePad`<br/>`SignCanvas` | • Khung cảm ứng cho người vay ký tay bằng ngón tay hoặc bút cảm ứng.<br/>• Hỗ trợ nút: Ký lại, Đồng ý.<br/>• Xuất chuỗi ảnh Base64 đính kèm khế ước nhận nợ. | • Khách hàng ký điện tử mượt mà, nét ký tự nhiên, không bị đứt nét.<br/>• Lưu trữ chữ ký số an toàn vào hồ sơ khế ước vay vốn.<br/>• Nhúng chữ ký vào mẫu hợp đồng tín dụng xem trước. |
| `TASK-AGENT-08.1`<br/>**Thu tiền gửi Tiết kiệm tại buôn làng** | `AgentSavingScreen`<br/>`SavingReceipt` | • Tìm kiếm sổ tiết kiệm theo tên thành viên / số NRC.<br/>• Nhập số tiền gửi góp định kỳ.<br/>• Ghi nhận giao dịch và in biên nhận gửi tiền qua máy in nhiệt Bluetooth. | • Thu tiền gửi tiết kiệm và in biên nhận tại chỗ cho người dân.<br/>• Tính năng cộng dồn số dư tức thời trên giao diện.<br/>• Lưu giao dịch vào hàng đợi đồng bộ an toàn. |
| `TASK-AGENT-08.2`<br/>**Mở sổ Tiết kiệm tích lũy tại chỗ** | `OpenSavingScreen`<br/>`SavingProduct` | • Nhập thông tin thành viên mở sổ, gói kỳ hạn, người thừa kế.<br/>• Nhập số tiền gửi ban đầu và lưu vào hàng đợi đồng bộ. | • Mở sổ tiết kiệm tích lũy cho người dân trực tiếp tại buổi sinh hoạt Cụm/Tổ.<br/>• Đăng ký đầy đủ thông tin người thừa kế hợp pháp.<br/>• In giấy chứng nhận mở sổ tạm thời cho người dân. |
| `TASK-AGENT-08.3`<br/>**Tiếp nhận Hồ sơ Bảo hiểm tương hỗ** | `AgentClaimScreen`<br/>`DocUploader` | • Khai báo loại rủi ro (Ốm đau, tai nạn, thiên tai).<br/>• Chụp ảnh giấy xác nhận của trưởng làng và hóa đơn bệnh viện.<br/>• Gửi hồ sơ về chi nhánh Township xét duyệt trợ cấp. | • Nộp hồ sơ trợ cấp tương hỗ trực tiếp từ thực địa buôn làng.<br/>• Đính kèm đầy đủ ảnh chứng từ y tế và biên bản xác nhận.<br/>• Cấp mã tra cứu hồ sơ bồi thường cho người dân an tâm. |
| `TASK-AGENT-08.4`<br/>**Quản lý Quỹ tiền mặt lưu động & QR nộp quỹ** | `CashScreen`<br/>`QrHandoverDialog` | • Hiển thị số tiền mặt MMK cán bộ đang giữ theo thời gian thực.<br/>• Cảnh báo màu đỏ nếu vượt hạn mức an toàn cho phép.<br/>• Sinh mã QR tổng kết thu tiền để thủ quỹ quét đối soát nhập két. | • Kiểm soát an toàn tồn quỹ tiền mặt và bàn giao quỹ cuối ngày nhanh chóng.<br/>• Khóa chức năng thu thêm tiền mặt nếu vượt quá hạn mức quy định.<br/>• Thủ quỹ quét mã QR tại quầy Township đối soát khớp 100% số tiền. |

---

### 1.3. Phân hệ BMF Customer App

| Mã & Tên Task | File / Class Chính | Nội dung & Luồng Xử lý Kỹ thuật | Tiêu chí Nghiệm thu Chi tiết (DoD) |
| :--- | :--- | :--- | :--- |
| `TASK-CUST-01.1`<br/>**Khởi tạo Flutter Customer & Theme BMF** | `pubspec.yaml`<br/>`CustomerTheme`<br/>`AppRouter` | • Khởi tạo dự án Flutter gọn nhẹ (< 30 MB).<br/>• Thiết lập giao diện Material 3 màu Xanh Navy `#1E3A8A` và Cam `#F59E0B`.<br/>• Cấu hình định tuyến navigation (`go_router`). | • Khởi chạy app mượt mà trên các dòng máy Android phân khúc giá rẻ.<br/>• Kích thước tệp APK phát hành < 25 MB, tối ưu cho mạng tải yếu.<br/>• Thiết kế giao diện thân thiện, cỡ chữ to rõ cho người cao tuổi. |
| `TASK-CUST-01.2`<br/>**Font Pyidaungsu & Song ngữ Myanmar/English** | `CustomerMy.arb`<br/>`CustomerEn.arb`<br/>`LanguageCubit` | • Tích hợp font Pyidaungsu chuẩn Unicode.<br/>• Xây dựng Cubit chuyển đổi ngôn ngữ không cần khởi động lại app.<br/>• Lưu ngôn ngữ lựa chọn vào `SharedPreferences`. | • Chuyển đổi qua lại giữa tiếng Myanmar và tiếng Anh tức thời.<br/>• Toàn bộ nhãn, thông báo, nút bấm được bản địa hóa 100%.<br/>• Tự động phát hiện ngôn ngữ mặc định của điện thoại. |
| `TASK-CUST-02.1`<br/>**Đăng ký Thành viên & OTP SMS** | `RegisterScreen`<br/>`OtpScreen`<br/>`AuthBloc` | • Form: Nhập số thẻ NRC và số điện thoại.<br/>• Gửi mã OTP SMS về số điện thoại đăng ký trong hợp đồng vay.<br/>• Xác thực OTP và kích hoạt tài khoản thành viên. | • Khách hàng đăng ký thành công tài khoản trong vòng dưới 2 phút.<br/>• Tự động điền mã OTP từ tin nhắn SMS trên Android.<br/>• Báo lỗi rõ ràng nếu số điện thoại không khớp với hồ sơ vay BMF. |
| `TASK-CUST-02.2`<br/>**Thiết lập mã PIN & Mở khóa Sinh trắc học** | `SetPinScreen`<br/>`BiometricService` | • Nhập và xác nhận mã PIN 6 số bảo mật.<br/>• Kích hoạt mở khóa vân tay / FaceID cho các lần đăng nhập tiếp theo. | • Thiết lập mã PIN thành công, đăng nhập nhanh bằng vân tay < 1 giây.<br/>• Bàn phím số xáo trộn ngẫu nhiên vị trí chống nhìn trộm mã PIN.<br/>• Bảo vệ an toàn tài khoản khi cho người khác mượn điện thoại. |
| `TASK-CUST-02.3`<br/>**Màn hình Trang chủ Khách hàng (Dashboard)** | `HomeScreen`<br/>`HomeBloc`<br/>`MemberCard` | • Hiển thị thẻ thành viên điện tử trực quan.<br/>• Thẻ tóm tắt khoản vay sắp đến hạn (Số tiền MMK, ngày đến hạn).<br/>• Menu lối tắt: Trả nợ MMQR, Mở sổ tiết kiệm, Yêu cầu bảo hiểm. | • Trang chủ tải dữ liệu nhanh < 300ms, giao diện tiếng Myanmar dễ hiểu.<br/>• Cảnh báo màu nổi bật khi có khoản nợ sắp đến hạn trong 3 ngày tới.<br/>• Hiển thị số dư tiền gửi tiết kiệm và điểm thành viên rõ ràng. |
| `TASK-CUST-03.1`<br/>**Danh sách Hợp đồng vay vốn đang hoạt động** | `LoanListScreen`<br/>`LoanCard` | • Hiển thị từng khoản vay dạng thẻ cuộn.<br/>• Hiển thị thanh tiến độ hoàn thành nợ.<br/>• Nút "Xem lịch trả nợ" và "Thanh toán ngay". | • Hiển thị đầy đủ thông tin các gói vay vốn của người dân.<br/>• Thanh tiến độ phần trăm hoàn thành khoản vay trực quan.<br/>• Phân loại rõ ràng các khoản vay cá nhân và vay theo nhóm tổ. |
| `TASK-CUST-03.2`<br/>**Chi tiết Lịch trả nợ Toàn khóa & 5 Nhóm nợ** | `LoanScheduleScreen`<br/>`ScheduleItem` | • Hiển thị chi tiết từng kỳ trả nợ từ kỳ 1 đến kỳ cuối.<br/>• Phân tách rõ ràng: Đã trả (Xanh lá), Đến hạn (Vàng), Quá hạn (Đỏ).<br/>• Hiển thị badge nhóm nợ theo chuẩn FRD Myanmar. | • Dữ liệu lịch nợ khớp 100% với số liệu kế toán trên máy chủ Core.<br/>• Hiển thị rõ số tiền gốc, lãi và phí bảo hiểm của từng kỳ.<br/>• Khách hàng nắm rõ tiến độ trả nợ và lịch sử đã thanh toán. |
| `TASK-CUST-04.1`<br/>**Thanh toán Nợ & Mã MMQR động** | `PaymentQrScreen`<br/>`QrRenderer` | • Gọi API lấy mã MMQR động chứa đúng số tiền MMK kỳ này.<br/>• Render mã QR lên màn hình kèm hướng dẫn quét mã.<br/>• Nút "Lưu ảnh QR vào thư viện" và "Mở ví điện tử". | • Hiển thị mã MMQR chuẩn xác, quét được trên tất cả app ngân hàng Myanmar.<br/>• Tự động điền đúng số tiền phải trả kỳ này, không lo chuyển sai tiền.<br/>• Cho phép chụp ảnh màn hình lưu mã QR để gửi cho người thân đóng hộ. |
| `TASK-CUST-04.2`<br/>**Deep Linking kết nối ví KBZPay** | `KbzLauncher`<br/>`UrlLauncher` | • Kiểm tra máy đã cài ứng dụng KBZPay chưa.<br/>• Mở thẳng app KBZPay (`kbzpay://qrpay?qrdata=...`) và truyền dữ liệu thanh toán.<br/>• Nếu chưa cài → Điều hướng sang Google Play / App Store. | • Bấm nút chuyển sang app KBZPay xác thực thanh toán mượt mà.<br/>• Tự động điền số tiền và mã hợp đồng trên màn hình thanh toán KBZPay.<br/>• Khách hàng chỉ cần nhập mã PIN ví điện tử là hoàn tất. |
| `TASK-CUST-04.3`<br/>**Deep Linking kết nối ví WavePay** | `WaveLauncher` | • Mở app WavePay (`wavepay://pay?qr=...`).<br/>• Tự động điền số tiền và mã tham chiếu thanh toán nợ BMF. | • Chuyển sang app WavePay thanh toán nhanh chóng.<br/>• Trải nghiệm thanh toán số liền mạch, không cần gõ lại thông tin tài khoản.<br/>• Tương thích hoàn hảo với các phiên bản WavePay mới nhất. |
| `TASK-CUST-04.4`<br/>**Deep Linking kết nối AYA Pay & MytelPay** | `AyaLauncher`<br/>`MytelLauncher` | • Hỗ trợ mở ví AYA Pay và MytelPay.<br/>• Bao phủ 100% các ví điện tử thông dụng tại thị trường Myanmar. | • Khách hàng có thể lựa chọn bất kỳ ví điện tử nào đang sử dụng.<br/>• Tự động kiểm tra danh sách app ví đã cài đặt trên máy người dùng.<br/>• Đa dạng hóa kênh thanh toán số cho người dân nông thôn. |
| `TASK-CUST-04.5`<br/>**Lắng nghe phản hồi gạch nợ & Biên lai điện tử** | `SuccessScreen`<br/>`PollingService` | • Polling trạng thái giao dịch mỗi 3 giây.<br/>• Khi gạch nợ thành công: Rung chuông, hiển thị màn hình chúc mừng.<br/>• Hiển thị biên nhận điện tử có mã tham chiếu và nút chia sẻ biên lai. | • Khách hàng nhận được thông báo xác nhận gạch nợ tức thời sau khi chuyển tiền.<br/>• Biên lai điện tử có giá trị đối soát pháp lý minh bạch.<br/>• Lịch nợ tự động cập nhật sang trạng thái "Đã thanh toán". |
| `TASK-CUST-05.1`<br/>**Sổ Tiết kiệm & Lãi dồn tích thực tế** | `SavingsScreen`<br/>`SavingCard` | • Hiển thị danh sách các sổ tiết kiệm tích lũy và bắt buộc.<br/>• Hiển thị số tiền lãi MMK dồn tích sinh ra hàng ngày.<br/>• Lịch sử các lần nộp tiền gửi và lãi nhập gốc. | • Khách hàng an tâm theo dõi số tiền tiết kiệm sinh lời mỗi ngày.<br/>• Thể hiện chi tiết số tiền gửi gốc, lãi suất năm và ngày đáo hạn.<br/>• Minh bạch hóa 100% dòng tiền tiết kiệm của người dân. |
| `TASK-CUST-05.2`<br/>**Mở Sổ Tiết kiệm Tích lũy Online** | `OpenSavingScreen`<br/>`SavingBloc` | • Chọn sản phẩm tiết kiệm và kỳ hạn gửi (3, 6, 12 tháng).<br/>• Nhập số tiền gửi ban đầu.<br/>• Chuyển sang luồng thanh toán ví điện tử để nộp tiền mở sổ. | • Mở sổ tiết kiệm thành công và kích hoạt ngay sau khi nạp tiền.<br/>• Khách hàng tự chủ gửi góp thêm tiền bất kỳ lúc nào từ ví điện tử.<br/>• Nhận chứng nhận mở sổ điện tử trực tiếp trên ứng dụng. |
| `TASK-CUST-06.1`<br/>**Tra cứu Quyền lợi Quỹ Tương trợ Thành viên** | `InsuranceScreen`<br/>`BenefitItem` | • Hiển thị chi tiết quyền lợi bảo hiểm tương hỗ mà thành viên được hưởng.<br/>• Hướng dẫn các bước chuẩn bị hồ sơ khi gặp sự cố rủi ro. | • Người dân nắm rõ quyền lợi an sinh của tổ chức tài chính vi mô.<br/>• Thông tin mức trợ cấp y tế, tai nạn được giải thích bằng tiếng Myanmar đơn giản.<br/>• Danh sách số điện thoại hỗ trợ khẩn cấp của chi nhánh Township. |
| `TASK-CUST-06.2`<br/>**Nộp Hồ sơ Trợ cấp Bảo hiểm đính kèm ảnh** | `ClaimScreen`<br/>`PhotoUploader` | • Chọn loại sự cố (Ốm đau, tai nạn, thiên tai).<br/>• Chụp ảnh hóa đơn viện phí hoặc xác nhận của chính quyền địa phương.<br/>• Theo dõi trạng thái duyệt chi trợ cấp (Đang thẩm định / Đã duyệt chi). | • Khách hàng tự nộp hồ sơ trợ cấp dễ dàng ngay tại nhà.<br/>• Hỗ trợ tải lên nhiều ảnh chứng từ rõ nét.<br/>• Nhận thông báo cập nhật tiến độ duyệt chi từ ban lãnh đạo BMF. |
| `TASK-CUST-07.1`<br/>**Firebase Messaging (FCM) Thông báo Đẩy** | `FcmService`<br/>`NotifyHandler` | • Đăng ký nhận FCM Token và lưu vào `SYS_MOBILE_DEVICE`.<br/>• Cấu hình kênh thông báo có âm thanh và cờ ưu tiên cao.<br/>• Bấm vào thông báo để mở đúng màn hình chi tiết món nợ. | • Nhận thông báo nhắc nợ tiếng Myanmar chuẩn xác cả khi app đang tắt.<br/>• Đăng ký kênh nhận thông báo theo khu vực địa bàn Township.<br/>• Âm thanh chuông báo riêng biệt đặc trưng của ứng dụng BMF. |
| `TASK-CUST-07.2`<br/>**Trung tâm Thông báo** | `NotifyListScreen`<br/>`NotifyItem` | • Hiển thị danh sách thông báo theo thứ tự thời gian mới nhất.<br/>• Đánh dấu Đã đọc / Chưa đọc.<br/>• Bộ lọc theo danh mục: Tài chính, Nhắc nợ, Tin tức. | • Quản lý lịch sử thông báo trực quan, không bị bỏ sót tin nhắc nợ.<br/>• Hỗ trợ tính năng "Đánh dấu tất cả là đã đọc".<br/>• Lưu trữ lịch sử thông báo trong 90 ngày gần nhất. |

---

### 1.4. Phân hệ CSDL Outbox và Tiến trình đồng bộ

| Mã & Tên Task | File / Class Chính | Nội dung & Luồng Xử lý Kỹ thuật | Tiêu chí Nghiệm thu Chi tiết (DoD) |
| :--- | :--- | :--- | :--- |
| `TASK-DB-01.1`<br/>**Tạo bảng CSDL `SYS_MOBILE_DEVICE`** | `V1.1_device.sql`<br/>CSDL `NG-mFINA` | • Viết script T-SQL tạo bảng `SYS_MOBILE_DEVICE`.<br/>• Tạo chỉ mục `IDX_DEVICE_USER` trên `(User_ID, User_Type, Is_Active)`.<br/>• Thiết lập giá trị mặc định cho các trường thời gian. | • Bảng được tạo thành công trên SQL Server 2017.<br/>• Truy vấn theo UserId có Index độ trễ < 1ms.<br/>• Đảm bảo toàn vẹn dữ liệu và khóa ngoại hợp lệ. |
| `TASK-DB-01.2`<br/>**Tạo bảng CSDL `SYS_OUTBOX_EVENT`** | `V1.2_outbox.sql` | • Viết script T-SQL tạo bảng `SYS_OUTBOX_EVENT`.<br/>• Tạo chỉ mục `IDX_OUTBOX_STATUS` trên `(Status, Created_Time)`.<br/>• Thêm trường `Payload_JSON` lưu dữ liệu mở rộng. | • Bảng Outbox sẵn sàng tiếp nhận hàng đợi sự kiện từ Database Triggers.<br/>• Chỉ mục tối ưu hóa cho câu lệnh quét theo trạng thái `PENDING`.<br/>• Không làm chậm tốc độ ghi của CSDL chính. |
| `TASK-DB-02.1`<br/>**Trigger `TRG_TD_GIAINGAN_OUTBOX` giải ngân** | `V1.3_trg_disb.sql` | • Trigger `AFTER INSERT ON TD_GIAINGAN`.<br/>• Lấy thông tin khách hàng và số tiền giải ngân từ bảng tạm `INSERTED`.<br/>• Ghi bản ghi sự kiện `LOAN_DISBURSED` với `Status = 'PENDING'`. | • Khi giao dịch viên giải ngân trên Desktop, sự kiện Outbox sinh ra < 0.5ms.<br/>• Hoàn toàn không can thiệp hay sửa đổi mã nguồn WCF Core hiện hữu.<br/>• Đảm bảo tính nguyên tử: Rollback nếu giao dịch chính bị lỗi. |
| `TASK-DB-02.2`<br/>**Trigger `TRG_TD_THUNO_OUTBOX` thu nợ quầy** | `V1.4_trg_pay.sql` | • Trigger `AFTER INSERT ON TD_THUNO`.<br/>• Trích xuất thông tin khách hàng và số tiền thu.<br/>• Ghi nhận sự kiện `PAYMENT_RECEIVED` chờ gửi thông báo cho người dân. | • Khách hàng được thông báo ngay khi giao dịch viên thu tiền tại quầy chi nhánh.<br/>• Bắt trọn vẹn các giao dịch thu nợ mặt, chuyển khoản và tất toán nợ.<br/>• Tốc độ thực thi trigger cực nhanh, không gây khóa bảng `TD_THUNO`. |
| `TASK-DB-02.3`<br/>**Trigger `TRG_TK_SOGD_OUTBOX` gửi/rút tiết kiệm** | `V1.5_trg_sav.sql` | • Trigger `AFTER INSERT ON TK_SOGD`.<br/>• Ghi nhận biến động số dư tiền gửi tiết kiệm vào bảng Outbox. | • Tự động sinh thông báo biến động số dư tiền gửi tiết kiệm.<br/>• Phân biệt rõ sự kiện nộp thêm tiền gửi và rút tiền tiết kiệm.<br/>• Ghi nhận chính xác số tiền MMK và số tài khoản sổ tiết kiệm. |
| `TASK-DB-03.1`<br/>**NotificationOutboxWorker trên Virtual Threads** | `OutboxWorker`<br/>`OutboxRepo` | • Sử dụng Spring `@Scheduled(fixedDelay = 1000)`.<br/>• Dùng Virtual Threads Executor quét lô 50 bản ghi `PENDING` từ `SYS_OUTBOX_EVENT`.<br/>• Tra cứu Token thiết bị trong `SYS_MOBILE_DEVICE`. | • Quét và phân phối thông báo liên tục mà không chiếm dụng CPU hay RAM.<br/>• Xử lý song song 50 thông báo đồng thời qua Virtual Threads cực nhanh.<br/>• Tự động tăng `retryCount` nếu gửi thất bại. |
| `TASK-DB-03.2`<br/>**Firebase Admin SDK Java đẩy Batch FCM** | `FcmService`<br/>`MsgFormatter` | • Định dạng nội dung tin nhắn bằng tiếng Myanmar Unicode.<br/>• Gửi hàng loạt qua Firebase FCM HTTP/2.<br/>• Cập nhật `Status = 'SENT'` và `Sent_Time = NOW()`. | • Độ trễ từ khi phát sinh sự kiện đến khi máy khách hàng rung thông báo < 1.5s.<br/>• Tỷ lệ gửi thành công đạt > 99.5% qua cổng FCM HTTP/2.<br/>• Tự động dọn dẹp các Device Token không còn hợp lệ khỏi CSDL. |
| `TASK-DB-04.1`<br/>**Cron Job quét nợ đến hạn 08:00 AM** | `LoanDueJob`<br/>`LoanDueService` | • Lịch chạy: `0 0 8 * * ?` hàng ngày.<br/>• Truy vấn các hợp đồng có kỳ nợ đến hạn trong khoảng `[Hôm nay + 1, Hôm nay + 3]`.<br/>• Sinh nội dung nhắc nợ tiếng Myanmar và đẩy qua Firebase FCM. | • 100% khách hàng sắp đến hạn nợ nhận được tin nhắn nhắc nợ vào 08:00 sáng.<br/>• Nội dung tin nhắn nêu rõ số tiền MMK cần chuẩn bị và ngày thu nợ.<br/>• Ghi nhật ký đầy đủ số lượng tin nhắn đã phát sinh trong ngày. |
| `TASK-DB-04.2`<br/>**ShedLock Redis bảo vệ Cron Job cụm server** | `ShedLockConfig`<br/>`ShedLockProvider` | • `@SchedulerLock(name = "LoanDueReminderTask", lockAtLeastFor = "15m", lockAtMostFor = "30m")`.<br/>• Khi chạy cụm nhiều container BFF, chỉ duy nhất 1 node chiếm khóa và thực thi. | • Tuyệt đối không xảy ra hiện tượng khách hàng nhận 2 lần tin nhắn nhắc nợ.<br/>• Khóa ShedLock được lưu an toàn trên Redis Cluster.<br/>• Tự động giải phóng khóa an toàn sau khi hoàn tất tác vụ quét nợ. |

---

### 1.5. Phân hệ An toàn thông tin và CI/CD

| Mã & Tên Task | File / Class Chính | Nội dung & Luồng Xử lý Kỹ thuật | Tiêu chí Nghiệm thu Chi tiết (DoD) |
| :--- | :--- | :--- | :--- |
| `TASK-SEC-01.1`<br/>**SSL Certificate Pinning trên Mobile Flutter** | `SslPinningClient`<br/>`CertConfig` | • Trích xuất SHA-256 Fingerprint của chứng thư số máy chủ Gateway.<br/>• Cấu hình kiểm tra chứng thư số trước mọi kết nối API.<br/>• Chặn kết nối nếu chứng thư số không khớp. | • Chặn đứng 100% công cụ can thiệp bắt gói tin (Burp Suite, Charles Proxy, Fiddler).<br/>• Ngăn chặn hoàn toàn các cuộc tấn công trung gian Man-in-the-Middle.<br/>• Hỗ trợ cấu hình chuỗi mã băm dự phòng khi gia hạn chứng thư số SSL. |
| `TASK-SEC-01.2`<br/>**Module kiểm tra Root / Jailbreak thiết bị** | `AntiTamperService`<br/>`SecurityChecker` | • Kiểm tra thiết bị có bị can thiệp bẻ khóa hệ thống không.<br/>• Nếu bị Root/Jailbreak → Hiển thị cảnh báo an ninh và thoát ứng dụng ngay lập tức. | • Ngăn chặn ứng dụng chạy trên các thiết bị đã bị bẻ khóa tiềm ẩn mã độc.<br/>• Phát hiện các công cụ can thiệp bộ nhớ (Frida, Magisk, Xposed Framework).<br/>• Bảo vệ an toàn dữ liệu tài chính của tổ chức và khách hàng. |
| `TASK-SEC-01.3`<br/>**Bật cờ `FLAG_SECURE` & Keystore chuỗi khóa** | `MainActivity.kt`<br/>`AppDelegate.swift`<br/>`KeyStorage` | • Bật `FLAG_SECURE` trên Android để chặn chụp màn hình / quay video màn hình.<br/>• Lưu chuỗi khóa SQLCipher trong Android Keystore và iOS Keychain. | • Không chụp được ảnh màn hình tài chính nhạy cảm của khách hàng.<br/>• Khóa giải mã CSDL SQLite được bảo vệ bằng phần cứng (Secure Enclave / TEE).<br/>• Tự hủy dữ liệu cục bộ nếu phát hiện bị tấn công can thiệp phần cứng. |
| `TASK-SEC-02.1`<br/>**Kịch bản k6 kiểm thử tải & Bẫy gạch nợ trùng** | `k6_double_pay.js`<br/>`k6_runner.sh` | • Kịch bản: 100 VUs bắn đồng thời 100 requests thu nợ cùng 1 món nợ `LN-2026-001`.<br/>• Chạy tải dồn dập trong 10 giây.<br/>• Câu lệnh SQL đối soát: `SELECT COUNT(*) FROM TD_THUNO WHERE Ma_Khach_Hang = 10001`. | • Đúng 1 request gạch nợ thành công, 99 requests còn lại bị từ chối an toàn.<br/>• Tổng số tiền thu trong CSDL khớp đúng số tiền của 1 kỳ nợ.<br/>• Hệ thống đạt độ trễ phản hồi P95 < 200ms dưới tải 1.000 RPS. |
| `TASK-SEC-02.2`<br/>**Kịch bản k6 kiểm thử tranh chấp số dư tiết kiệm** | `k6_saving_concur.js` | • Kịch bản: 50 requests nộp tiền gửi đồng thời vào 1 sổ tiết kiệm (mỗi món 10.000 MMK).<br/>• Đối soát CSDL: Số dư cuối = Số dư đầu + 500.000 MMK.<br/>• Sai số tài chính bằng đúng 0 MMK. | • Số dư tiền gửi tiết kiệm khớp 100%, không bị lỗi Dirty Read hay Lost Update.<br/>• Không phát sinh hiện tượng Deadlock trên CSDL SQL Server.<br/>• Tất cả 50 giao dịch nộp tiền đều được ghi nhận đầy đủ vào sổ phụ. |
| `TASK-SEC-03.1`<br/>**Pipeline CI/CD GitHub Actions cho Java BFF** | `bff-ci-cd.yml`<br/>`Dockerfile` | • Cấu hình Action chạy `mvn clean test`.<br/>• Build Docker image Java 21 đa tầng (Multi-stage build).<br/>• Đẩy image lên Container Registry và cập nhật container `micro-finance-bff`. | • Tự động hóa 100% quy trình build và deploy Backend Gateway khi merge code.<br/>• Unit Test Coverage đạt ≥ 80% trước khi cho phép đóng gói image.<br/>• Thời gian hoàn tất toàn bộ pipeline build và deploy < 3 phút. |
| `TASK-SEC-03.2`<br/>**Pipeline Fastlane tự động đóng gói Mobile** | `Fastfile`<br/>`Appfile`<br/>`Matchfile` | • Cấu hình Fastlane tự động tăng mã build version.<br/>• Ký số chứng thư số phát hành (Keystore / Apple Distribution Certificate).<br/>• Xuất bản tệp APK và đẩy bản build lên TestFlight. | • Đóng gói bản cài đặt APK/IPA tự động chỉ bằng 1 lệnh `fastlane release`.<br/>• Tự động tải bản build lên Google Play Internal Track và Apple TestFlight.<br/>• Gửi thông báo hoàn tất đóng gói vào kênh trao đổi nội bộ của đội dự án. |

---

## 2. LỘ TRÌNH TRIỂN KHAI THEO SPRINT (BACKEND & SYNC FIRST)

Chiến lược triển khai theo nguyên tắc **Backend & Sync First**: Hoàn thiện toàn bộ hạ tầng Backend BFF Gateway, CSDL SQL Server Outbox, Database Triggers, SOAP CXF Adapters, Cổng thanh toán MMQR/Ví điện tử, Outbox Worker và kiểm thử tải Backend trong **Sprint 1 đến Sprint 5 (Tuần 1 - 10)**. Sau khi Backend & Sync hoàn thiện và sẵn sàng 100%, toàn bộ đội ngũ Mobile tập trung phát triển **BMF Agent App (Sprint 6 - 9)** và **BMF Customer App (Sprint 10 - 12)** kết nối trực tiếp vào API thật.

```
========================================================================================
GIAI ĐOẠN 1: HOÀN THIỆN BACKEND BFF GATEWAY, CSDL OUTBOX & ĐỘNG CƠ ĐỒNG BỘ (TUẦN 1 - 10)
========================================================================================

TUẦN 1 - 2 (Sprint 1: Kiến trúc Nền tảng BFF Java 21, CSDL Thiết bị & Bảng Outbox):
├── Backend Team: TASK-BFF-01.1 (Clean Architecture) → TASK-BFF-01.2 (Virtual Threads & OpenAPI) → TASK-BFF-01.3 (GlobalHandler)
├── Database Team: TASK-DB-01.1 (SYS_MOBILE_DEVICE) → TASK-DB-01.2 (SYS_OUTBOX_EVENT)
└── DevOps Team: TASK-SEC-03.1 (Pipeline CI/CD GitHub Actions & Docker Java 21 BFF)

TUẦN 3 - 4 (Sprint 2: Xác thực Bảo mật, Quản lý Phiên & Kết nối CSDL NG-mFINA):
├── Backend Team: TASK-BFF-02.1 (Device Repo) → TASK-BFF-02.2 (JwtProvider JJWT) → TASK-BFF-02.3 (Redisson Blacklist)
├── Backend Team: TASK-BFF-02.4 (Login Cán bộ) → TASK-BFF-02.5 (Login Khách hàng NRC) → TASK-BFF-02.6 (Refresh & Logout)
└── Database Team: TASK-BFF-03.1 (HikariCP NG-mFINA) → TASK-BFF-03.2 (LoanJdbcRepo Cụm/Tổ & Lịch nợ)

TUẦN 5 - 6 (Sprint 3: WCF SOAP Adapter, Khóa Phân tán & Toàn bộ API Cán bộ Thực địa):
├── Backend Team: TASK-BFF-03.3 (CXF TinDungClient) → TASK-BFF-03.4 (CXF KeToanClient & TietKiemClient)
├── Backend Team: TASK-BFF-04.1 (IdempotencyFilter) → TASK-BFF-04.2 (Redisson RLock) → TASK-BFF-04.3 (RateLimitingFilter)
└── Backend Team: TASK-BFF-05.1 (API Centers) → TASK-BFF-05.2 (API Schedule) → TASK-BFF-05.3 (API Collect Nợ) → TASK-BFF-05.4 (API Apply Vay) → TASK-BFF-05.5 (API Tiết kiệm) → TASK-BFF-05.6 (API Bảo hiểm) → TASK-BFF-05.7 (API Handover QR)

TUẦN 7 - 8 (Sprint 4: Toàn bộ API Khách hàng, Cổng MMQR & Webhook Ví Điện tử):
├── Backend Team: TASK-BFF-05.8 (API Khách hàng Loans) → TASK-BFF-05.9 (API Schedule 5 nhóm nợ) → TASK-BFF-05.10 (API Tiết kiệm & Lãi tích lũy) → TASK-BFF-05.11 (API Mở sổ online) → TASK-BFF-05.12 (API Claim Bảo hiểm)
└── Backend Team: TASK-BFF-06.1 (MMQR Generator EMVCo) → TASK-BFF-06.2 (Webhook KBZPay) → TASK-BFF-06.3 (Webhook WavePay) → TASK-BFF-06.4 (Webhook AYA Pay & MytelPay)

TUẦN 9 - 10 (Sprint 5: Database Triggers Outbox, Virtual Threads Worker & Stress Test BE):
├── Database Team: TASK-DB-02.1 (Trigger Giải ngân) → TASK-DB-02.2 (Trigger Thu nợ quầy) → TASK-DB-02.3 (Trigger Tiết kiệm)
├── Backend Team: TASK-DB-03.1 (Notification Outbox Worker) → TASK-DB-03.2 (Firebase Admin FCM Batch) → TASK-DB-04.1 (Cron Job Nhắc nợ 08:00 AM) → TASK-DB-04.2 (ShedLock Redis Cluster)
└── QA / Test Lead: TASK-SEC-02.1 (k6 Double Pay Test) → TASK-SEC-02.2 (k6 Saving Concurrency Test)
    >>> CỘT MỐC: Nghiệm thu hoàn thiện 100% Backend Mobile BFF & Outbox Sync Engine, sẵn sàng cho Mobile <<<

========================================================================================
GIAI ĐOẠN 2: PHÁT TRIỂN BMF AGENT APP - OFFLINE-FIRST & IN BLUETOOTH (TUẦN 11 - 18)
========================================================================================

TUẦN 11 - 12 (Sprint 6: Khung Nền tảng Agent App, Font Myanmar & CSDL SQLite Mã hóa):
├── Mobile Team: TASK-AGENT-01.1 (Flutter FSD Clean Architecture) → TASK-AGENT-01.2 (Font Pyidaungsu & i18n) → TASK-AGENT-01.3 (DioClient & Retry)
├── Mobile Team: TASK-AGENT-02.1 (Drift ORM & SQLCipher AES-256) → TASK-AGENT-02.2 (Bảng danh mục Cụm/Tổ) → TASK-AGENT-02.3 (Bảng giao dịch & Sync Queue)
└── DevOps Team: TASK-SEC-03.2 (Pipeline Fastlane tự động đóng gói Mobile Android/iOS)

TUẦN 13 - 14 (Sprint 7: Nghiệp vụ Cán bộ, Bảng kê Thu nợ & Lập Phiếu Thu Tiền mặt MMK):
├── Mobile Team: TASK-AGENT-03.1 (Login Cán bộ) → TASK-AGENT-03.2 (Ràng buộc Thiết bị & Sinh trắc học)
└── Mobile Team: TASK-AGENT-04.1 (Dashboard Cán bộ) → TASK-AGENT-04.2 (Danh sách Cụm/Tổ) → TASK-AGENT-04.3 (Bảng kê Thu nợ) → TASK-AGENT-04.4 (Form Lập phiếu thu MMK) → TASK-AGENT-04.5 (Ghi nhận giao dịch SQLite & Sync Queue)

TUẦN 15 - 16 (Sprint 8: In Nhiệt Bluetooth Tiếng Myanmar & Động cơ Đồng bộ Ngoại tuyến 2 Chiều): [HOÀN THÀNH 100%]
├── Mobile Team: TASK-AGENT-05.1 (Quét & Kết nối Máy in BLE) → TASK-AGENT-05.2 (Render Bitmap Hóa đơn tiếng Myanmar) → TASK-AGENT-05.3 (Lệnh in ESC/POS Raster)
└── Mobile Team: TASK-AGENT-06.1 (SyncBloc & Network Monitor) → TASK-AGENT-06.2 (Pull Sync Tải danh mục) → TASK-AGENT-06.3 (Push Batch Sync Đẩy giao dịch) → TASK-AGENT-06.4 (UI Trạng thái Đồng bộ)
    >>> CỘT MỐC: Đóng gói BMF Agent App MVP v1.0 thử nghiệm in và thu nợ ngoại tuyến thực địa (34/34 Tests PASS) <<<

TUẦN 17 - 18 (Sprint 9: Thẩm định Tín dụng OCR NRC, Khảo sát GPS, Tiết kiệm & Quản trị Quỹ): [HOÀN THÀNH 100%]
├── Mobile Team: TASK-AGENT-07.1 (Camera OCR NRC) → TASK-AGENT-07.2 (Regex Bóc tách NRC Myanmar) → TASK-AGENT-07.3 (Khảo sát Nhà & GPS) → TASK-AGENT-07.4 (Ký hợp đồng e-Sign)
└── Mobile Team: TASK-AGENT-08.1 (Thu Tiết kiệm Buôn làng) → TASK-AGENT-08.2 (Mở Sổ Tiết kiệm tại chỗ) → TASK-AGENT-08.3 (Hồ sơ Bảo hiểm tương hỗ) → TASK-AGENT-08.4 (Quản lý Quỹ tiền mặt & QR Nộp quỹ)
    >>> CỘT MỐC: Hoàn thành toàn diện BMF Agent App v2.0 (65/65 Mobile Tests PASS 100%, 37/37 Backend Tests PASS 100%) <<<

========================================================================================
GIAI ĐOẠN 3: BMF CUSTOMER APP, BẢO MẬT, PENTEST, UAT & GO-LIVE (TUẦN 19 - 24)
========================================================================================

TUẦN 19 - 20 (Sprint 10: Khởi tạo Customer App, eKYC Thẻ NRC, Tra cứu Hợp đồng & Lịch Nợ): [HOÀN THÀNH 100%]
├── Mobile Team: TASK-CUST-01.1 (Khởi tạo Flutter Customer & Theme BMF) → TASK-CUST-01.2 (Font Pyidaungsu & Song ngữ Myanmar/English)
├── Mobile Team: TASK-CUST-02.1 (Đăng ký Thành viên OTP SMS) → TASK-CUST-02.2 (Thiết lập PIN & Sinh trắc học) → TASK-CUST-02.3 (Dashboard Khách hàng)
└── Mobile Team: TASK-CUST-03.1 (Danh sách Khoản vay Đang hoạt động) → TASK-CUST-03.2 (Chi tiết Lịch trả nợ & 5 Nhóm nợ FRD)

TUẦN 21 - 22 (Sprint 11: Thanh toán MMQR, Deep Linking Ví Điện tử, Tiết kiệm & Push FCM): [HOÀN THÀNH 100%]
├── Mobile Team: TASK-CUST-04.1 (Mã MMQR động EMVCo) → TASK-CUST-04.2 (Deep Link KBZPay) → TASK-CUST-04.3 (Deep Link WavePay) → TASK-CUST-04.4 (Deep Link AYA Pay/MytelPay) → TASK-CUST-04.5 (Polling Phản hồi Gạch nợ & Biên lai)
├── Mobile Team: TASK-CUST-05.1 (Sổ Tiết kiệm & Lãi dồn tích) → TASK-CUST-05.2 (Mở Sổ Tiết kiệm Online) → TASK-CUST-06.1 (Quyền lợi Bảo hiểm) → TASK-CUST-06.2 (Nộp Hồ sơ Trợ cấp Y tế)
└── Mobile Team: TASK-CUST-07.1 (Firebase FCM Push Notification tiếng Myanmar) → TASK-CUST-07.2 (Trung tâm Thông báo)
    >>> CỘT MỐC: Hoàn thành BMF Customer App v1.0 (35/35 Tests PASS 100%) <<<

TUẦN 23 - 24 (Sprint 12: Đóng gói Bảo mật Mobile, Pentest Độc lập, UAT Thực địa & Bảo mật): [HOÀN THÀNH 100%]
├── Security Team: TASK-SEC-01.1 (SSL Certificate Pinning) → TASK-SEC-01.2 (Module Anti-Root/Jailbreak) → TASK-SEC-01.3 (FLAG_SECURE & Keystore/Keychain)
│   • Áp dụng đồng bộ trên cả BMF Agent App & BMF Customer App (SslPinningClient SHA-256, AntiTamperService, ScreenProtectionService)
│   • Bằng chứng kiểm thử: 10/10 Security Tests PASS (5 Agent + 5 Customer)
├── QA / Pentest: Thực thi kịch bản đo kiểm an toàn thông tin & bẫy gạch nợ trùng/tranh chấp số dư
└── TỔNG KẾT HỆ THỐNG: 142/142 Tests PASS 100% (Backend: 37/37, Agent App: 70/70, Customer App: 35/35)

TUẦN 25 - 26 (Sprint 13: Tối ưu UI/UX, Đa ngôn ngữ English Mặc định, Chuẩn hóa Dữ liệu Seed & Đóng gói Release): [HOÀN THÀNH 100%]
├── Mobile Team: Rà soát và tinh chỉnh 100% giao diện (loại bỏ lỗi xuống dòng, rút gọn nhãn nút bấm, cân đối chiều ngang trên cả Customer App và Agent App).
├── Mobile Team: Thiết lập Tiếng Anh (English) làm ngôn ngữ mặc định trong LanguageCubit, hỗ trợ chuyển đổi mượt mà sang Tiếng Myanmar và Tiếng Việt.
├── Database Team: Chuẩn hóa 100% dữ liệu Seed Data (V1.7, V1.11) sang tiếng Anh quốc tế, loại bỏ hoàn toàn tiếng Việt trên CSDL SQL Server 35.247.156.176.
├── Mobile Team: Bật R8 Minification, ProGuard rules, Resource Shrinking và Tree-shaking tối ưu dung lượng tệp tin APK.
│   • BMF Customer App Release APK: Giảm từ 177MB xuống 18.0MB.
│   • BMF Agent App Release APK: Giảm từ 186MB xuống 21.0MB.
│   • Xuất bản 6 tệp APK Release vào outputs/apks/ (Universal, ARM64, ARMv7).
└── Documentation Team: Hoàn thiện hồ sơ tài liệu giới thiệu sản phẩm docs/mobile_app_proposal.md, đính kèm 14 ảnh chụp màn hình thực chứng 100% từ thiết bị thật.
```

> [!IMPORTANT]
> **Phân định Ranh giới Phạm vi Triển khai:**
> * **Phase 1 (Hiện tại - Đã nghiệm thu 100%):** Hoàn thành toàn diện 2 ứng dụng di động BMF Customer & BMF Agent, Backend BFF Gateway Java 21 Spring Boot 3.3, cơ chế ngoại tuyến SQLite Offline-First, thu nợ Cụm/Tổ buôn làng, in hóa đơn Bluetooth, định danh MMQR nội bộ, mã PIN Scramble và gói APK Release siêu nhẹ.
> * **Phase 2 & Phase 3 (Roadmap tương lai):** Tích hợp cổng thanh toán trực tuyến bên ngoài, liên kết API ví điện tử (KBZPay, WavePay, AYA Pay) và xác thực định danh điện tử eKYC với cơ sở dữ liệu dân cư quốc gia.


